<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PAJAApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Change the status bar to black from purple -->
        <item name="android:statusBarColor">@android:color/black</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Primary brand color. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/my_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/my_secondary</item>
        <item name="android:textColor">@color/my_text</item>
        <item name="colorTertiary">@color/my_tertiary</item>

    </style>

    <style name="Theme.PAJAApp" parent="Base.Theme.PAJAApp" />
</resources>